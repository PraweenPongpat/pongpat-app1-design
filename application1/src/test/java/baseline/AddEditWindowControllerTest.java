package baseline;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class AddEditWindowControllerTest {

    @Test
    void doneButtonPushed() {
        //the return value of doneButtonPushed is an ItemInATodoList object

        //manually create one ItemInATodoList object with known value (expected,add)
        //simulate the GUI passing in the empty string to set the scene
        //to initialize should not display anything but the default text in each TextField
        //enter the information from GUI simulation (actual)

        //assertEqual the two objects expected and actual

    }

    @Test
    void cancelButtonPushed() {
        //an expected return Object is null (expected)

        //simulate the cancelButtonPush from that scene (actual)

        //assertEqual the two (should both be null)

    }
}