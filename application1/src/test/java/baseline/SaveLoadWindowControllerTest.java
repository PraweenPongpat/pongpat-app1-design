/*
 *  UCF COP3330 Fall 2021 Application Assignment 1 Solution
 *  Copyright 2021 Praween Pongpat
 */

package baseline;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class SaveLoadWindowControllerTest {

    @Test
    void finishButtonPushed() {
        //when the finishButton is pushed, it will return a list of strings [fileName, fileLocation]
        //simulate the GUI calling the finished Button Pushed (actual)

        //manually made the list that contains the same fileName and fileLocation (expected)

        //assertEqual the both expected and actual (may need a loop to compare each element)
    }
}